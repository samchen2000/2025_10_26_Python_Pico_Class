# 這是大標題
## 這是次標題
### 這是次次標題
#### 這是次次次標題

下面是git的初始化設定

```base
我是一個程式碼
git config --global user.name "SamChen"
git config --global user.email "sampitcher1976@gmail.com"
git config --global pull.rebase false

```