## 💻 第三天範例程式：day3_loops.py
### 📚 學習主題：流程控制 (下) - 迴圈
迴圈允許我們重複執行一段程式碼，這對於處理大量數據或執行重複性任務來說是不可或缺的。

🎯 重點內容
- for 迴圈：通常用於已知次數或遍歷序列（如列表、字串）的情況。

- range() 函式：生成數字序列，常用於 for 迴圈。

- while 迴圈：通常用於未知次數，只要條件為真就持續執行的情況。

- break 與 continue：控制迴圈的流程。
```
# 檔案名稱: day3_loops.py
# 這是 Sam 老師為初學者準備的第三天課程範例！
# 目標：學習 for 迴圈、while 迴圈，以及 range() 函式。

# --- 區塊一：for 迴圈與 range() 函式 ---
print("--- 1. for 迴圈基礎 ---")

# range(n) 會產生從 0 到 n-1 的數字序列
# 這裡會從 0, 1, 2, 3, 4 執行五次
print("A. 倒數計時範例 (從 0 到 4):")
for i in range(5):
    # i 在每次迴圈中都會自動更新
    print(f"目前計數: {i}")

# range(start, stop, step) 也可以指定起始值和步長
print("\nB. 偶數輸出範例 (從 2 到 10, 步長為 2):")
for number in range(2, 11, 2):
    print(f"偶數: {number}")

# 應用範例：計算 1 到 5 的總和
total_sum = 0
for k in range(1, 6): # 產生 1, 2, 3, 4, 5
    total_sum = total_sum + k # 也可以簡寫成 total_sum += k
    
print(f"\n1 到 5 的總和是: {total_sum}")


# --- 區塊二：while 迴圈 ---
print("\n--- 2. while 迴圈基礎 ---")

# while 迴圈會在條件 (condition) 保持為 True 時持續執行
countdown_timer = 3
print("C. while 迴圈倒數:")
while countdown_timer > 0:
    print(f"還有 {countdown_timer} 秒...")
    countdown_timer -= 1 # 必須確保條件最終會變成 False，否則會變成無窮迴圈！

print("倒數結束！")

# 應用範例：簡單的登入嘗試 (直到成功為止)
correct_password = "samsam"
attempt = 0
max_attempts = 3

while attempt < max_attempts:
    user_input = "samsam" # 這裡假設使用者輸入了正確密碼，實際應用會用 input()
    # user_input = input("請輸入密碼: ") # 實際應用時
    
    if user_input == correct_password:
        print("密碼正確，登入成功！")
        break # 💡 break 函式：立即跳出整個迴圈
    else:
        attempt += 1
        print(f"密碼錯誤，請重新嘗試。您還有 {max_attempts - attempt} 次機會。")

# 檢查是否因為嘗試次數用盡而失敗
if attempt == max_attempts:
    print("嘗試次數過多，帳號被鎖定。")


# --- 區塊三：continue 函式 ---
print("\n--- 3. continue 函式範例 ---")
# continue 函式：跳過本次迴圈中 continue 後面的程式碼，直接進入下一次迴圈
print("只顯示奇數 (跳過偶數):")
for num in range(1, 11): # 產生 1 到 10
    if num % 2 == 0: # 檢查是否為偶數 (餘數為 0)
        continue # 如果是偶數，跳過 print(num) 這一行，直接進入下一個數字
    
    print(num) # 只有奇數會被印出


# === 區塊四：後續預留程式碼 (方便未來擴展) ===

# TODO: 未來可以加入巢狀迴圈 (Nested Loops) 的練習，例如輸出九九乘法表。
# for i in range(1, 10):
#     for j in range(1, 10):
#         print(f"{i}*{j}={i*j}", end='\t')
#     print() # 換行

# TODO: 結合昨天學的 if 判斷和今天的迴圈，寫一個判斷質數的程式。
# (例如：用迴圈檢查一個數字是否能被 2 到它本身-1 之間的任何數整除)
```